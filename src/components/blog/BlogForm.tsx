"use client";
import { FormEvent, useState } from "react";
import { BlogPost } from "@/api/blogApi";
export function BlogForm({ post, onSubmit }: { post?: BlogPost; onSubmit: (data: FormData) => Promise<void> }) { const [saving, setSaving] = useState(false); const [error, setError] = useState(""); const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setSaving(true); setError(""); try { await onSubmit(new FormData(event.currentTarget)); } catch (err: unknown) { setError(err instanceof Error ? err.message : "Could not save post."); } finally { setSaving(false); } }; return <form onSubmit={submit} className="mx-auto grid max-w-3xl gap-4 p-6"><input name="title" required defaultValue={post?.title} placeholder="Title" className="rounded border p-3" /><textarea name="excerpt" defaultValue={post?.excerpt} placeholder="Short excerpt" className="min-h-24 rounded border p-3" /><textarea name="content" required defaultValue={post?.content} placeholder="Write your article…" className="min-h-72 rounded border p-3" /><label className="text-sm">Cover image <input name="image" type="file" accept="image/*" className="ml-2" /></label><label className="text-sm"><input name="published" type="checkbox" value="true" defaultChecked={post?.published ?? true} className="mr-2" />Publish now</label>{error && <p className="text-red-700">{error}</p>}<button disabled={saving} className="rounded bg-[#0f5132] px-5 py-3 text-white disabled:opacity-60">{saving ? "Saving…" : "Save post"}</button></form>; }
